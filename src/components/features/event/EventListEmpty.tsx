import React from "react";

// 빈 상태 컴포넌트
const EventListEmpty = () => (
  <div className="w-full min-h-screen flex items-center justify-center">
    <div className="text-lg text-gray-600">진행중인 이벤트가 없습니다.</div>
  </div>
);

export default EventListEmpty;
